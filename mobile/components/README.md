# Components Directory

Reusable, modular React Native UI components.

## Structure
Each component should be in its own subfolder:
- `ComponentName/`
  - `index.tsx` (Implementation)
  - `types.ts` (Type definitions)
  - `ComponentName.test.tsx` (Unit tests)
  - `styles.ts` (Optional: styling)

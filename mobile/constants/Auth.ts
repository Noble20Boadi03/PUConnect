/** Secure storage key for the JWT session token. */
export const AUTH_TOKEN_KEY = 'auth_token';

/** Secure storage key for the user's theme preference. */
export const THEME_PREFERENCE_KEY = 'theme_preference';

/** Secure storage key indicating whether the user has completed onboarding. */
export const HAS_COMPLETED_ONBOARDING_KEY = 'has_completed_onboarding';

/** Secure storage key indicating whether this is the user's first login session (needs to complete photo setup). */
export const IS_FIRST_LOGIN_SESSION_KEY = 'is_first_login_session';

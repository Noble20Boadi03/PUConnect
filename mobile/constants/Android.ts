/**
 * Keep in sync with `android.edgeToEdgeEnabled` in app.json.
 * When true, the system nav bar is transparent — use safe-area padding, not
 * NavigationBar.setBackgroundColorAsync.
 */
export const ANDROID_EDGE_TO_EDGE_ENABLED = true;

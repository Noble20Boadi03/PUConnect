# App Directory

This directory is exclusively for screen components and file-based navigation per Expo Router specifications.

## Conventions
- Use `index.tsx` for the root route of a directory.
- Use `[param].tsx` for dynamic routes.
- Layouts should be named `_layout.tsx`.
- Keep screen-specific logic here or delegate to hooks/services.

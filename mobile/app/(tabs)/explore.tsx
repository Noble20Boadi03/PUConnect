import React from 'react';
import { ExploreView } from '../../components';

/**
 * Explore — browse service categories and discover providers on campus.
 * Pure UI; category drill-down and profile navigation wire up later.
 */
export default function ExploreScreen() {
  return <ExploreView />;
}

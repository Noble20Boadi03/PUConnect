# Config Directory

Environment-specific configuration and feature flag management.

## Guidelines
- Separate development, staging, and production configurations.
- Use `expo-constants` or `react-native-dotenv` for environment variable loading.
- Manage feature flags here.

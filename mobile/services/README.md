# Services Directory

Centralized API client implementations and external service integrations.

## Structure
- Separate concerns by creating distinct files for each backend domain or third-party service.
- Includes data transformation logic.
- Examples: `api.ts`, `auth.service.ts`, `payment.service.ts`.
